name: ReorgTest
description: Local Anvil reorg detection test (PostgreSQL storage)
project_type: no-code
networks:
- name: polygon
  chain_id: 137
  rpc: http://127.0.0.1:8545
storage:
  postgres:
    enabled: true
    drop_each_run: true
contracts:
- name: USDC
  details:
  - network: polygon
    address: "__TOKEN_ADDRESS__"
    start_block: "0"
  abi: ./abis/MockTransfer.abi.json
  reorg_safe_distance: false
  tables:
  - name: replay_balances
    columns:
    - name: holder
      type: address
    - name: total_in
      default: "0"
    events:
    - event: Transfer
      operations:
      - type: upsert
        set:
        - column: holder
          action: set
          value: $to
        - column: total_in
          action: add
          value: $value
        where:
          holder: $to
  include_events:
  - Transfer
